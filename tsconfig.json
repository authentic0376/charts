{
  "compilerOptions": {

    "module": "ESNext", // 또는 "ES2022", "ES2020" 등
    "target": "ESNext", // 최신 JS 기능 사용 가능하도록 설정 권장
    "moduleResolution": "bundler", // Vite 권장 설정

    // import.meta 를 사용하기 위해 lib 에 "ESNext" 또는 해당 기능이 포함된 버전 추가 필요할 수 있음
    // 보통 target/module 설정 시 자동으로 포함되지만 명시적으로 추가 가능
    "lib": ["ESNext", "DOM", "DOM.Iterable"],
  },
  "include": ["src/**/*.ts", "src/**/*.d.ts", "src/**/*.tsx", "src/**/*.vue"],
}